{
  "name": "@tenu/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "U2 foundation — encrypted local SQLite, identity, settings, transactional writes, demo seed.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@tenu/domain": "workspace:*",
    "better-sqlite3-multiple-ciphers": "^13.0.3",
    "zod": "^4.6.5"
  }
}
